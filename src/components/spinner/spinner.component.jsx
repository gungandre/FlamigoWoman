import { CustomLoader, SpinnerContainer } from "./spinner.styles";

const Spinner = () => {
  return (
    <SpinnerContainer>
      <CustomLoader />
    </SpinnerContainer>
  );
};

export default Spinner;
